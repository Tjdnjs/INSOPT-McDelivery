import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

const getCart = async () => {};

const putCart = async () => {};

const cartService = {
  getCart,
  putCart,
};

export default cartService;
